"""Models package.

After the Phase 1 cleanup this package only carries training_config
(consumed by scripts/fetch_data.py) and the saved artifacts in
models/checkpoints/. The old model wrapper classes were removed —
see plan.md, section 1.2.
"""
